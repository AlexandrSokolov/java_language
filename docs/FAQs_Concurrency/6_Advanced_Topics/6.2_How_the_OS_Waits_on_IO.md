## s
C. Who waits instead
8. If no thread waits, who does?
9. Which thread runs the next stage? (→ 4.3)
10. Why keep slow work off the event loop?
11. Where does a paused request live?
12. What ends a long-waiting HTTP call?


6.2: how the waiting is done (below your code)

C. Who waits instead: 8, 10, 11, 12
Trap: CPU work on the event loop (goes with card 10)
New OS cards, candidates:
What is a file descriptor?
What does the kernel keep for a socket?
What happens before the server sees a connection?
Does a socket write wait for the network?
What happens to a thread blocked on a read?
How does one thread watch many sockets?
Why does epoll scale better than select?
What does a context switch cost?
Why is opening a connection expensive?
What does keep-alive change?

"Blocked on a read" and "context switch" may overlap 1.5, so check for duplicates. 
If 6.2 grows past about 15 cards, it splits cleanly again: kernel mechanics (fd, socket, epoll, scheduling) 
vs connection lifecycle (handshake, keep-alive, timeouts).

## q